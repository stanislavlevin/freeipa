parameters:
    options: ''

steps:
- script: |
    set -e
    echo "Running autoconf generator"
    ./autogen.sh \
        ${{ parameters.options }} \
        --enable-pylint \
        --with-password-quality-lib=no \

  displayName: Configure the project
