steps:
- script: |
    set -e
    sudo dnf -y install python3-pip
    python3 -m pip install --user --constraint .wheelconstraints.in --ignore-installed git+https://github.com/PyCQA/pylint.git@7bb50436
  displayName: Install Lint dependencies
